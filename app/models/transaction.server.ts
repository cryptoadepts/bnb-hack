import type { ethers } from "ethers";

export function createCollectionTransaction({
  collectionId,
  userId,
}: {
  collectionId: string;
  userId: string;
}): ethers.Transaction {
  return {} as any;
}
